<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DevCrew.Desktop.ViewModels"
             x:Class="DevCrew.Desktop.Views.Base64DecoderView"
             x:DataType="vm:Base64DecoderViewModel">

    <ScrollViewer>
        <Border Padding="{DynamicResource ResponsivePaddingMedium}" Background="{StaticResource BrushSecondaryBackground}">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto" HorizontalAlignment="Stretch">

                <StackPanel Grid.Row="0" Spacing="8" Margin="0,0,0,16">
                    <TextBlock Text="{Binding [base64decoder.title], Source={StaticResource Loc}}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="{StaticResource BrushPrimaryText}"
                               HorizontalAlignment="Center" />

                    <TextBlock Text="{Binding [base64decoder.subtitle], Source={StaticResource Loc}}"
                               FontSize="14"
                               Foreground="{StaticResource BrushSecondaryText}"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Row="1"
                        Background="{StaticResource BrushTertiaryBackground}"
                        BorderBrush="{StaticResource BrushBorderSecondary}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12"
                        DragDrop.AllowDrop="True"
                        x:Name="InputBorder">
                    <Grid RowDefinitions="Auto,Auto,Auto" ColumnDefinitions="*,Auto">
                        <TextBlock Grid.Row="0"
                                   Grid.Column="0"
                                   Text="{Binding [base64decoder.input_title], Source={StaticResource Loc}}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource BrushSecondaryText}"
                                   VerticalAlignment="Center" />

                        <Button Grid.Row="0"
                            Grid.Column="1"
                            Content="{Binding [base64decoder.browse], Source={StaticResource Loc}}"
                                Command="{Binding BrowseFileCommand}"
                                Padding="10,6"
                                FontWeight="SemiBold"
                                Classes="ActionButton PrimaryActionButton"
                                ToolTip.Tip="{Binding [base64decoder.browse_tooltip], Source={StaticResource Loc}}" />

                        <TextBlock Grid.Row="1"
                                   Grid.ColumnSpan="2"
                                   Text="{Binding SelectedFileDisplayName}"
                                   IsVisible="{Binding HasSelectedFile}"
                                   FontSize="11"
                                   Margin="0,6,0,0"
                                   Foreground="{StaticResource BrushSecondaryText}"
                                   TextWrapping="Wrap" />

                        <TextBox Grid.Row="2"
                                 Grid.ColumnSpan="2"
                                 Text="{Binding InputBase64, Mode=TwoWay}"
                                 AcceptsReturn="True"
                                 MinHeight="180"
                                 MaxHeight="400"
                                 Margin="0,8,0,0"
                                 Padding="8"
                                 Classes="MonospaceInputTextBox"
                                 FontFamily="{StaticResource FontMonospace}"
                                 FontSize="{StaticResource FontSizeSmall}"
                                 TextWrapping="Wrap"
                                 Watermark="{Binding [base64decoder.input_watermark], Source={StaticResource Loc}}" />
                    </Grid>
                </Border>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8" Margin="0,16,0,16">
                    <Button Content="{Binding [base64decoder.decode], Source={StaticResource Loc}}"
                            Command="{Binding DecodeCommand}"
                            IsEnabled="{Binding HasInput}"
                            Classes="ActionButton PrimaryActionButton"
                            FontWeight="SemiBold"
                            ToolTip.Tip="{Binding [base64decoder.decode_tooltip], Source={StaticResource Loc}}" />
                </StackPanel>

                <Border Grid.Row="3"
                        Background="{StaticResource BrushTertiaryBackground}"
                        BorderBrush="{StaticResource BrushBorderSecondary}"
                        BorderThickness="1"
                        CornerRadius="6"
                        Padding="12">
                    <Grid RowDefinitions="Auto,Auto,*">
                        <Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto" Margin="0,0,0,8">
                            <TextBlock Grid.Column="0"
                                       Text="{Binding [base64decoder.output_title], Source={StaticResource Loc}}"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource BrushSecondaryText}"
                                       VerticalAlignment="Center" />

                                <Button Grid.Column="1"
                                    Content="{Binding [base64decoder.clear], Source={StaticResource Loc}}"
                                    Command="{Binding ClearCommand}"
                                    Margin="0,0,8,0"
                                    Padding="10,6"
                                    FontWeight="SemiBold"
                                    Classes="ActionButton DangerActionButton"
                                    ToolTip.Tip="{Binding [base64decoder.clear_tooltip], Source={StaticResource Loc}}" />

                                <Button Grid.Column="2"
                                    Content="{Binding [base64decoder.save], Source={StaticResource Loc}}"
                                    Command="{Binding SaveBinaryCommand}"
                                    IsEnabled="{Binding HasDecodedOutput}"
                                    Padding="10,6"
                                    FontWeight="SemiBold"
                                    Classes="ActionButton NeutralActionButton"
                                    ToolTip.Tip="{Binding [base64decoder.save_tooltip], Source={StaticResource Loc}}" />
                        </Grid>

                        <Border Grid.Row="1"
                                Padding="8"
                                CornerRadius="4"
                                Margin="0,0,0,8"
                                IsVisible="{Binding StatusMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding IsError, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#5F2D2D|#2D5F2E'}" Opacity="0.2" />
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="{Binding IsError, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#F44336|#4CAF50'}" />
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>

                            <TextBlock Text="{Binding StatusMessage}"
                                       FontSize="12"
                                       FontWeight="Medium"
                                       TextWrapping="Wrap">
                                <TextBlock.Foreground>
                                    <SolidColorBrush Color="{Binding IsError, Converter={StaticResource BoolToColorConverter}, ConverterParameter='#F44336|#4CAF50'}" />
                                </TextBlock.Foreground>
                            </TextBlock>
                        </Border>

                        <Grid Grid.Row="2" MinHeight="80">
                            <TextBox Text=""
                                     IsReadOnly="True"
                                     Padding="8"
                                     MinHeight="80"
                                     Classes="MonospaceInputTextBox"
                                     VerticalContentAlignment="Center"
                                     IsVisible="{Binding !HasDecodedOutput}"
                                     Watermark="{Binding [base64decoder.output_watermark], Source={StaticResource Loc}}" />

                            <StackPanel Spacing="12"
                                        Margin="4,4,4,8"
                                        IsVisible="{Binding HasDecodedOutput}">

                                <Grid ColumnDefinitions="*,12,*,12,*">

                                    <!-- MIME Type -->
                                    <Border Grid.Column="0"
                                            Background="{StaticResource BrushSecondaryBackground}"
                                            BorderBrush="{StaticResource BrushBorderSecondary}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="12,10">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="{Binding [base64decoder.mime_type], Source={StaticResource Loc}}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource BrushSecondaryText}" />
                                            <TextBlock Text="{Binding DetectedMimeType}"
                                                       FontSize="12"
                                                       FontWeight="Medium"
                                                       Foreground="{StaticResource BrushPrimaryText}"
                                                       TextWrapping="Wrap"
                                                       FontFamily="{StaticResource FontMonospace}" />
                                        </StackPanel>
                                    </Border>

                                    <!-- Extension -->
                                    <Border Grid.Column="2"
                                            Background="{StaticResource BrushSecondaryBackground}"
                                            BorderBrush="{StaticResource BrushBorderSecondary}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="12,10">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="{Binding [base64decoder.extension], Source={StaticResource Loc}}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource BrushSecondaryText}" />
                                            <TextBlock Text="{Binding DetectedExtension}"
                                                       FontSize="12"
                                                       FontWeight="Medium"
                                                       Foreground="{StaticResource BrushPrimaryText}"
                                                       FontFamily="{StaticResource FontMonospace}" />
                                        </StackPanel>
                                    </Border>

                                    <!-- Size -->
                                    <Border Grid.Column="4"
                                            Background="{StaticResource BrushSecondaryBackground}"
                                            BorderBrush="{StaticResource BrushBorderSecondary}"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="12,10">
                                        <StackPanel Spacing="4">
                                            <TextBlock Text="{Binding [base64decoder.size], Source={StaticResource Loc}}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource BrushSecondaryText}" />
                                            <TextBlock Text="{Binding FormattedFileSize}"
                                                       FontSize="12"
                                                       FontWeight="Medium"
                                                       Foreground="{StaticResource BrushPrimaryText}"
                                                       FontFamily="{StaticResource FontMonospace}" />
                                        </StackPanel>
                                    </Border>

                                </Grid>

                                <TextBlock Text="{Binding [base64decoder.save_hint], Source={StaticResource Loc}}"
                                           FontSize="12"
                                           Foreground="{StaticResource BrushSecondaryText}"
                                           TextWrapping="Wrap"
                                           Margin="4,0" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </ScrollViewer>
</UserControl>
